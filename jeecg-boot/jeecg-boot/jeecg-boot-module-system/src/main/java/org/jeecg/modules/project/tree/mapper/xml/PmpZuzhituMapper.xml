<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.project.tree.mapper.PmpZuzhituMapper">

    <select id="addMyNode" resultType="java.lang.String" statementType="CALLABLE">
	   {call dbo.AddSubNode(
	    #{Id,jdbcType=VARCHAR,mode=IN},
	    #{parentNodeId,jdbcType=VARCHAR,mode=IN},
	    #{taskId,jdbcType=VARCHAR,mode=IN}
	    )}
	</select>

</mapper>